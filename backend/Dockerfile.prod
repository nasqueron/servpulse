FROM node:20-alpine

RUN addgroup -S servpulse && adduser -S servpulse -G servpulse

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY . .

RUN chown -R servpulse:servpulse /app
USER servpulse

EXPOSE 3000

CMD ["node", "app.js"]
